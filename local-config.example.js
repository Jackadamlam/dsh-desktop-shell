// Copy this file to local-config.js and edit the path.
// local-config.js is git-ignored, so your machine-specific path never gets committed.
module.exports = {
  DSH_PROJECT_DIR: 'C:/path/to/deepseek-harness',
};
